function gathernNamesOf(arrayOfPeople){
  return arrayOfPeople.map(person => person.age)
}

const people = [{name: 'Volt', name: 'Just', name: 'Hurts'}];

const = names = gathernNamesOf(people);

export {gathernNamesOf}
